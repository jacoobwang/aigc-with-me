# AIGC With Me Content Context

这个上下文描述 AI 工具目录中的内容、提交、审核和更新语言，帮助区分自动发现的外部信息与已经对用户公开的目录内容。

## Content lifecycle

**内容更新提案**：针对已有目录条目提出的一组待审核内容变化，作为一个不可拆分的审核单元整体接受或整体拒绝。它描述建议如何变化，但在被接受前不改变用户正在看到的正式条目。
_Avoid_: 自动覆盖、直接刷新、无审核更新

**内容字段**：描述工具本身的信息字段，包括名称、链接、介绍、分类、标签、图标和截图；内容更新提案可以建议修改这些字段，但名称和链接的应用仍需要遵守条目标识规则。
_Avoid_: 状态字段、运营字段

**运营字段**：决定条目是否发布、如何审核、是否赞助或如何曝光的字段，例如 `publishDate`、计划状态、`sponsor`、`featured` 和 `forceHidden`。自动复查提案不负责改变这些字段。
_Avoid_: 内容字段

**内容快照**：从 item 官方链接抓取并标准化后的外部页面内容及其 hash，用来判断来源是否发生变化，不是目录对外展示的正文。
_Avoid_: 聚合页、当前 item 文案

**实质差异**：标准化后会改变目录内容语义或用户看到的素材的差异；空白、HTML 格式、标点和其他无意义噪声不属于实质差异。
_Avoid_: 任意字符串差异

**更新提案文档**：独立保存一组针对 item 的待审核内容变化、来源快照和审核结果的内容文档；它不是正式 item 的替代品。
_Avoid_: item 草稿、临时字段

**过期提案**：创建后所依据的正式 item 内容已经发生变化，因此不能再直接应用的内容更新提案。
_Avoid_: 失败提案、已拒绝提案
